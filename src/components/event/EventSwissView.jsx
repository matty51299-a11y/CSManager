export default function EventSwissView({ tournament, model }) {
  return <div className="swiss-hero"><div className="round-focus"><span className="live-chip">SWISS LIVE</span><h2>Round {model.activeRound?.number || tournament.swiss.rounds.length + 1} qualification race</h2><p>Win three to qualify. Lose three and you are out. Pairings are generated from current records with rematch avoidance.</p><div className="progress-lanes"><div><b>{tournament.swiss.qualified.length}</b><span>Qualified</span></div><div><b>{model.standings.filter((s)=>s.status==='active').length}</b><span>Alive</span></div><div><b>{tournament.swiss.eliminated.length}</b><span>Eliminated</span></div></div></div><div className="standings-snapshot">{model.standings.slice(0,10).map((s,i)=><div className={s.teamId===model.userTeamId?'is-user':''} key={s.teamId}><span>#{i+1}</span><b>{s.team.shortName}</b><em>{s.wins}-{s.losses}</em><small className={`status-${s.status}`}>{s.status}</small></div>)}</div></div>;
}
