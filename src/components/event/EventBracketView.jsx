export default function EventBracketView({ tournament, userTeamId, onSimUser }) {
  return <div className="bracket-overlay">{tournament.playoffs?.rounds.map((round,ri)=><section key={round.name}><h3>{round.name}</h3>{round.matches.map((match,mi)=>{const isUser=[match.teamA?.teamId,match.teamB?.teamId].includes(userTeamId); return <div className={`bracket-node ${isUser?'is-user':''}`} key={mi}><div><span>{match.teamA?.shortName || 'TBD'}</span><b>{match.result?.mapsWonA ?? '-'}</b></div><div><span>{match.teamB?.shortName || 'TBD'}</span><b>{match.result?.mapsWonB ?? '-'}</b></div>{match.result ? <small>{match.result.winner.shortName} advanced</small> : isUser ? <button onClick={()=>onSimUser({ roundIndex:ri, matchIndex:mi })}>Sim This Match</button> : <small>Upcoming</small>}</div>;})}</section>)}</div>;
}
